cmake_minimum_required(VERSION 2.6)

file(GLOB GEOMETRY_SOURCE *.cc *.h)

include_directories(${PROJECT_SOURCE_DIR}/Common)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})

add_library(geometry ${GEOMETRY_SOURCE})
